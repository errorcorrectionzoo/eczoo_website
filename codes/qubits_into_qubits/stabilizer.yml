# UTF-8 unicode encoding

code_id: stabilizer

physical: qubits

logical: qubits

name: 'Stabilizer code'

description: 'An \(n,2^k,d\) stabilizer is usually denoted as \([[n,k,d]]\). Logical subspace is the joint \(+1\) eigenspace of a set of commuting Pauli operators forming the code''s stabilizer group.'

protection: ''

features: '(nonlocal?) mapping to CSS codes?'

decoder: ''  

relations:

